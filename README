dotfiles
========

    git clone https://github.com/matsuyoshi30/dotfiles


Emacs
-----

symbolic link to home directory

    ln -s $(pwd)/.emacs.d ~/
    cd .emacs.d
    emacs --batch -f batch-byte-compile init.el


Zsh
---

symbolic link to home directory

    ln -s $(pwd)/.zsh/.zprofile ~/
    ln -s $(pwd)/.zsh/.zshrc ~/


Tmux
----

symbolic link to home directory

    ln -s $(pwd)/tmux.conf ~/


Docker
------

    docker run --rm -it -v $PWD:/dev -w /dev --tmpfs=/tmp/tmpfs:rw,size=500m,mode=1777 dev
